---
title: "TEDDY Multi-Assay Analysis"
author: "Tim Vigers & Laura Pyle"
date: "`r format(Sys.time(), '%d %B %Y')`"
output:
  html_document:
    number_sections: yes
    toc: yes
    toc_depth: 2
    toc_float: yes
---

```{r setup, include=FALSE}
library(mlr3verse)
library(tidyverse)
knitr::opts_chunk$set(echo = FALSE)
home_dir = ifelse(.Platform$OS.type != "unix","T:/",
                  "/mnt/UCD/PEDS/RI Biostatistics Core/Shared/Shared Projects/Laura/BDC/Projects/Howard Davidson/TEDDY data")
knitr::opts_knit$set(root.dir = home_dir)
```

```{r load full df}

```

```{r mlr3 test}
df = data.frame(cbind(raw_data$Carotenoids$Time_0$X,raw_data$Carotenoids$Time_0$Y$y))
colnames(df)[ncol(df)] = "y"
task_test = TaskRegr$new(id = "test", 
                     target = "y",
                     backend = df)
autoplot(task_test, type = "pairs")
```

```{r}
task_test$data()
```

